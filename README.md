Incoming CS student, learning to build things properly rather than just following tutorials.

Currently: a CSV-to-Parquet data pipeline (CLI, tests, Docker, a working UI), and Python fundamentals via CS50P.

Feedback on the code is welcome — still learning.
